[pytest]
# The build runs as `python make/`, which puts make/ on sys.path and lets its
# modules import each other flat (`from standings import ...`). Tests import
# them the same way. `..` is for `build.mongo` / `build.settings`, and for the
# sibling data repos (metadata, parse).
pythonpath = . .. make
testpaths = tests
